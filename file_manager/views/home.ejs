<!DOCTYPE html>
<html lang="<%= req.language %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= req.t('dashboard_title') %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1><%= req.t('dashboard_title') %></h1>
  </header>
  
  <div class="hero">
    <h2><%= req.t('dashboard_hero') %></h2>
    <p><%= req.t('dashboard_description') %></p>
    <a href="/register" class="cta-button"><%= req.t('get_started') %></a>
  </div>
</body>
</html>
